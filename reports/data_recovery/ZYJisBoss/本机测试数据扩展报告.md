# 本机测试数据扩展报告

生成时间：2026-09-18T20:02:03+08:00
本机标识：`LEGION`
GitHub 用户名：`ZYJisBoss`

## 扫描范围

已识别固定磁盘：`C:`、`D:`、`E:`。仅扫描明确的项目目录；未扫描 Windows、软件安装目录、游戏内容或其他无关个人资料。

- `C:\Users\zgz20\Desktop\代码\C-TURE`
- `C:\Users\zgz20\Desktop\第六赛道大件运输\第六赛道相关文献\六轴车数据`
- `E:\BaiduNetdiskDownload\使命召唤\C-TURE`
- `E:\BaiduNetdiskDownload\使命召唤\OnSite-B-1150-Unified`

指定但本机不存在的目录：`D:\generic_route_lab`、`D:\Onsite-6`、`D:\Onsite-5`、`D:\OnSite`、`D:\研`。

## 主数据集去重确认

基线：`D:\微信\微信聊天\xwechat_files\wxid_amt5qtexvhih22_9cd6\msg\file\2026-09\主数据集去重基线.csv`，共 359 条去重候选。55条本机有效候选的最终分层如下：

- 与主数据集完全重复：14 条。
- 同路线但有新报告：0 条。
- 真正新增且有布尔型硬标签：0 条。
- 确认新增但无逐样本硬标签：32 条。
- 仍无法确认：9 条。
- 55条有效候选中无法解析：0 条；另有1个未进入55条集合的损坏桌面副本。

本机输入哈希与基线算法兼容：否。已用相同文件哈希对照验证；因此本轮没有用不兼容的输入哈希判重。完全重复均由 `route_file_sha256` 确认；组合签名命中但缺少主数据主要数组摘要的路线保守列入 `unresolved_candidates.csv`。

## 报告与硬标签续查

- `summary.json`：0；可关联到55条路线的逐样本 `safety_summary` / `validation_report` / certificate：0。
- 含 `hard_certificate_passed` 字段的 JSON：1 份，但它是部署聚合摘要，不是逐样本报告，未拆分或伪造标签。
- 因此 `confirmed_new_labeled_samples.csv` 只有表头。

## 文件与结构统计

- 审计 NPZ：91 份；可解析候选文件 67 份；本机去重后有效路线55条；本机路径级重复组 12 组。
- 车型/标定配置文件：66 份；结构化配置证据 63 份。

- 六轴显式：8；六轴结构推断：19。
- 五轴显式：27；五轴结构推断：0。
- 未知或冲突：1。

## 地图名称核对

本机出现28个地图/路线名称；基线25张地图之外的名称为：turnaround_1_dynamic, turnaround_4_tuning, turnaround_8796_template。
这3个名称均是既有 `turnaround` 路线的动态、调参或模板文件名，名称本身不能证明新地图。因此真正确认超出主数据集25张地图的名称：无。

## 车辆配置与标定版本

63组证据中的显式车型族均为基线已有的 `five_axis` / `six_axis`；确认新增车型或车辆指纹：0 组。
发现4个具名标定包版本：20260806-longitudinal-v6-upto25, five-axis-1150-zip-20260816, official-physics-restore-20260816-candidate, onsite5-active-runtime-20260827。基线没有标定版本或配置文件哈希字段，因此这些只能标为“候选新标定版本”，不能仅凭现有基线确认新增。
逐文件结论见 `vehicle_configuration_evidence.csv` 的比较状态列。

## 建议后续集中复制

原清单建议复制 76 个文件，共 38902607 字节。其中确认新增且有硬标签的样本所必需文件：0 个；确认新增无标签路线所必需文件：32 个，共 20169688 字节。
这些必需文件在 `local_file_inventory.csv` 中标记为 `confirmed_new_required=true`，并逐条列于 `confirmed_new_unlabeled_routes.csv`；完全重复文件不属于新增样本必需传输项。

## 损坏、扫描失败与缺失信息

- `C:\Users\zgz20\Desktop\代码\C-TURE\five_axis\race_guidance_control\routes\five_axis_ay_v1_release\ramp_3.npz`：BadZipFile: File is not a zip file
- 未找到逐样本 `summary.json` / `safety_summary.json`，因此无法把部署摘要中的聚合通过数分配给具体路线，也未据此伪造标签。
- 基线输入哈希算法与本机审计脚本的本地输入哈希算法不同；在负责人提供算法实现前，不将两者直接比较。
- 基线不含主要数组摘要，组合签名命中项保持未决，没有强行选取一个基线样本。
- 控制器、轮胎、载荷和地图构建版本在不少文件中缺失；空值保持为空，未根据路径虚构。

## 可复现方式

在仓库根目录使用包含 NumPy 的 Python 运行：

```powershell
python analysis/data_recovery/ZYJisBoss/run_local_data_audit.py --baseline "D:\微信\微信聊天\xwechat_files\wxid_amt5qtexvhih22_9cd6\msg\file\2026-09\主数据集去重基线.csv"
```

脚本只读原始目录和基线文件，并重新生成清单与本报告。
